{
  "name": "image-api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "node ./run-dev.js",
    "postinstall": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt || (python -m venv .venv && .venv\\Scripts\\pip.exe install -r requirements.txt)"
  }
}