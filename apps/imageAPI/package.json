{
  "name": "image-api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "postinstall": "python3 -m venv .venv || py -3 -m venv .venv && .venv/Scripts/python.exe -m pip install -r requirements.txt || .venv/bin/python -m pip install -r requirements.txt",
    "dev": ".venv/bin/python -m uvicorn app.main:app --reload --port 8000 || .venv/Scripts/python.exe -m uvicorn app.main:app --reload --port 8000"
  }
}
